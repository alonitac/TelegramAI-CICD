apiVersion: apps/v1
kind: Deployment
metadata:
  name: bot-deployment
  labels:
    app: bot
    env: 
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bot
      env: 
  template:
    metadata:
      labels:
        app: bot
        env: 
    spec:
      containers:
      - name: bot
        image: 
        resources:
          requests:
            memory: "300Mi"
            cpu: "500m"
          limits:
            memory: "300Mi"
            cpu: "500m"
        